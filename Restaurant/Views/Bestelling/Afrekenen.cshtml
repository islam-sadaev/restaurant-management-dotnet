@model AfrekenenListViewModel
@{
    ViewData["Title"] = "afrekenen";
}
<h1>Reservaties</h1>
<table class="table">
   @{/* <tr>
        <td>
            @Html.DisplayFor(v => v.Reservaties[0].Datum)
        </td>
        <td>
            @Html.DisplayFor(v => v.Reservaties[0].AantalPersonen)
        </td>
        @foreach (var order in Model.Reservaties[0].Bestellingen/*.Where( b => b.StatusId == 3))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelitem => order.Productnaam)
                </td>
                <td>
                    @Html.DisplayFor(modelitem => order.Aantal)
                </td>
                <td>
                    @Html.DisplayFor(modelitem => order.Prijs)
                </td>
            </tr>
        }
        
        <td>
        <button class="btn btn-primary" type="button" asp-action="AfrekenenBestelling">Contant</button>
        </td>
        <td>
        <button class="btn btn-primary" type="button" asp-action="AfrekenenBestelling"><img src="/images/payconiq-20210305.png" /></button>
        </td>
    </tr>*/}
    @foreach (var item in Model.Reservaties)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelitem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelitem => item.AantalPersonen)
            </td>
            @foreach (var order in item.Bestellingen)
            {
            <tr>
                <td>
                        @Html.DisplayFor(modelitem => order.Productnaam)
                </td>
                <td>
                        @Html.DisplayFor(modelitem => order.Prijs)
                </td>
            </tr>
            }
        <td>
                @Html.DisplayFor(modelitem => item.TotaalPrijs)
        </td>

        <td>
            <button class="btn btn-primary" type="button" asp-action="Bevestiging">Contant</button>
        </td>
        <td>
            <button class="btn btn-primary" style="width: 20%; height: 20%;" type="button" asp-action="Bevestiging"><img src="/images/payconiq-20210305.png" /></button>
        </td>
        </tr>
    }
</table>

